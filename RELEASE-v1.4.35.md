# Release v1.4.35

- Refine the fixed single-column sidebar so long navigation labels no longer get clipped.
- Widen the sidebar and force disclosure-row labels to wrap inside the available width.
- Preserve the single-column navigation layout across supported window sizes.
