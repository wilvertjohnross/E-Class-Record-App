# KLAS v1.4.27

Adds the bundled book-and-sun logo to the left of KLAS, enlarges desktop banner typography by 75%, adds a slow shine effect with reduced-motion support, uses the tagline The One Place for Every Klass, and removes Works offline. Smaller windows scale typography to fit. Includes prior welcome layout changes.

Browser checks cover popup/profile persistence, greetings/clock, navigation, icons, removed strips, themes and responsive widths, reduced motion and print. Source/payload parity and signing script syntax checked. Signed update installation not tested.
