v1.1.4 — SF2 Diagonal Compliance Marks + Signature Centering
- Late Comer/Tardy now shades the upper triangular half formed by the official dashed diagonal.
- Cutting Classes now shades the lower triangular half formed by the official dashed diagonal.
- Absent preserves the official dashed rising diagonal and adds only the opposite descending diagonal to form an X.
- Adviser and School Head names are centered across their existing signature lines in generated SF2 copies.
- SF2 row compaction, attendance calculations, SF9 feed, backup compatibility, ECR, GS, and official template files are unchanged.

VERSION 1.1.3 — SF2 DEPED-COMPLIANT MARKINGS / COMPACT PRINT ROWS
- Restored the original SF2 official template used in v1.1.0/v1.1.1 (SHA-256 2fe5668f6fd86ce7cb7a1cc2f76ac2e6e35bc20d3b80d779ce459000c0c9d4ba).
- Restored the four-state attendance workflow: blank = Present, full-cell X = Absent, upper-half shaded = Late Comer/Tardy, lower-half shaded = Cutting Classes.
- Click cycle: Present -> Absent -> Late/Tardy -> Cutting Classes -> Present. Keyboard: P/Space, X/A, L/T, C.
- The working grid now draws the mark geometrically across the whole attendance box instead of relying on text glyphs.
- Official SF2 output keeps attendance cells text-free and overlays edge-to-edge diagonal lines for Absent and exact half-cell shading for Late/Tardy and Cutting Classes.
- Unused Male/Female learner rows are hidden only in each generated SF2 copy so preview/print height follows the current SF1/current roster while all official total, summary, legend, signature, formatting, and print settings remain intact.
- Existing v1.1.0/v1.1.1/v1.1.2 SF2 data remains readable. Literal T values normalize to the existing internal L tardy code; legacy C values remain Cutting Classes.
- Grading/transmutation, ECR, GS, SF9 calculations, JSON persistence, updater security, and official ECR/GS templates are unchanged.

VERSION 1.1.2 — SIMPLIFIED SF2 X / T ATTENDANCE MARKING
- Replaced the bundled SF2 template with the newly uploaded workbook without editing its static content or layout.
- Changed the SF2 click cycle to Present (blank) → Absent (X) → Tardy (T) → Present.
- Removed Cutting Classes from the current UI/keyboard marking workflow and removed half-cell attendance symbols.
- Uses one whole-day mark per learner per day; no AM/PM split.
- Official SF2 generation now writes uppercase X for Absent and T for Tardy into the daily cell.
- Preserved the v1.1.x JSON schema by storing new tardy marks using the existing internal L code; legacy C records are normalized to Tardy when read.
- Backup import remains compatible with v1.1.0/v1.1.1 SF2 attendance records.
- No grading, ECR, GS, SF9, IPC allowlist, updater-signature, or persistence-architecture changes.

VERSION 1.1.1 — SF2 INPUT / METADATA REGRESSION FIX
- Fixed the v1.1.0 metadata validator regression that rejected sf2Enabled because it is a boolean.
- Boolean metadata is now accepted by both SF2 and shared ECR/GS official-output payload validation.
- Restores Official SF2 output and prevents sf2Enabled from breaking Official ECR/Grading Sheet generation.
- Preserves v1.1.0 SF2 JSON data and v1.0.27-compatible data without migration.
- No changes to grading/transmutation logic, attendance calculations, SF9 logic, or official template files.
- Validation notices remain non-blocking.

VERSION 1.1.0 — SF2 DAILY ATTENDANCE INTEGRATION
- Added opt-in SF2 / Advisory Class setting.
- Added SF2 Attendance tab with month selector, selectable school days, fast daily marking, learner remarks, and monthly summary.
- Added P/A/L/C app-side attendance model and official SF2 symbol conversion.
- Added automatic SF2 → SF9 monthly attendance synchronization; SF9 attendance fields are locked for months already maintained in SF2 to prevent conflicting records.
- Added official SF2 XLSX generation from the supplied DepEd template, with external-link residue and broken #REF defined names removed only in generated copies.
- Added SF2 Excel/PDF preview cache and Print/Open-in-Excel actions.
- Added SF2 backup validation, learner-delete cleanup, capacity validation, and 5-consecutive-absence calculation.
- Added SF2 runtime IPC actions to the trusted main-process allowlist.
- Versioning policy now treats feature additions as minor releases; v1.0.27 remains the final v1.0.x build.

v1.0.27 — Dark-mode visual optimization (cumulative full package)
- Adds centrally managed semantic dark-theme tokens.
- Improves contrast, surface hierarchy, borders, disabled states, focus treatment, status colors, and dark-mode table readability.
- Preserves official-template exports/printing and grading logic.
- Includes v1.0.26 PS/WS two-decimal display behavior.
- Includes v1.0.25 raw-score recovery/non-blocking validation behavior.

v1.0.26 — Two-decimal PS/WS display
- Displays PS and WS consistently to two decimal places in working views and official outputs.
- No change to the underlying grading/transmutation rules.

v1.0.25 — Raw-score input recovery hotfix
- Replaces blocking raw-score validation alerts with a non-modal notice.
- Invalid/out-of-HPS entries are rolled back without breaking subsequent score entry.
- The invalid field is briefly highlighted; no modal focus trap is used.
- Cumulative application state includes v1.0.21–v1.0.24 fixes.

Version 1.0.20 — Stability & Security Gate
- Consolidated v1.0.19 clean-slate ECR runtime into full bootstrap.
- Reliable save acknowledgement + recovery copy + revision-aware local recovery.
- Raw-score/HPS/weight validation and official-output release gate.
- Exact official-ECR custom-component rounding architecture.
- CSP, navigation lockdown, trusted-main-renderer IPC checks, backup validation.
- Added 25 MB safety caps to official SF1/ECR imports and 10 MB to roster CSV import.
- Removed the retired bootstrap ECR export fallback; one runtime official-output engine is authoritative.
- Signed Ed25519 .ecrupdate verification and strict package integrity checks.
- adm-zip 0.6.0.
- Preview cache signature validation and duplicate-click suppression.
- Roster CSV imports are transactional and bounded; oversized/unterminated CSV data is rejected without partially modifying the roster.
- Backup validation now rejects malformed or injected school-logo data URIs.
- SF1/score-import learner-cap checks hardened.

v1.0.9
- Added local Downloads-folder development auto-update channel (.ecrupdate).
- Updates stage automatically without interrupting the running app and activate on the next normal launch.
- Added Help > Check Downloaded Updates and update-aware About information.
- Added footer runtime/staged-version status.
- Added stable runtimeInvoke bridge so future downloaded runtime packages can add main-process features without changing preload.
- Added runtime resource overlay support for app UI and official templates.
- Preserved all v1.0.8 SF1/ECR/GS/SF9 features and local data storage.

v1.0.8
- Added Import Official SF1 (.xls) to the Roster tab.
- Reads the official legacy Excel SF1 file directly; CSV conversion is not required.
- Detects the LRN/NAME/Sex/Birth Date/Age columns from the official SF1 header.
- Imports learner name, 12-digit LRN, age and sex; birth date is retained in the learner record for future use.
- Treats SF1 as the authoritative learner-identity source and matches by LRN first, then safe normalized-name fallback.
- Preserves existing learner IDs so Term grades, SF9 other grades, attendance and comments remain connected after an SF1 refresh.
- Adds missing learners automatically and reorders the roster to the official SF1 order.
- Includes an optional, OFF-by-default control to remove current learners not listed in the uploaded SF1.
- Parses .xls internally and does not require Microsoft Excel for SF1 importing.
- The user's uploaded SF1 is never bundled into the app package.

v1.0.7
- Replaced the Class Record CSV score-import button with Import Official ECR (.xlsx).
- Imports an already-filled workbook that uses the same official ECR template bundled with the app.
- Reads the detected term, class/school metadata, HPS, weights, learner names/sex blocks, and raw WW/PT/EXAM scores directly from fixed template cells.
- Works even when the current class roster is empty; imported learner names are added automatically.
- Existing learners are matched by normalized name; raw scores in the detected term are replaced from the workbook while other terms remain untouched.
- App grading logic recalculates PS, WS, Initial Grade, Term Grade and Descriptor from imported raw scores.
- Cached Excel Term Grades, when available, are used only as a verification check and discrepancy count.
- Rejects workbooks that do not match the official ECR layout and reports unresolved external-name-link cases clearly.
- Removed the obsolete Class Record score-import CSV example from the project package.

v1.0.6
- Added Official ECR Print Preview using desktop Microsoft Excel on Windows.
- The Class Record print action now generates the filled official template and opens Excel directly in Print Preview.
- This preserves the official workbook print area, scaling, margins, logos and page setup.
- If Excel Print Preview is unavailable, the filled workbook opens normally as a fallback.

E-Class Record App with GS and SF9 — v1.0.5

NEW
- Bundled the supplied official Class Record Excel template under templates/.
- Added Download Official ECR (.xlsx) in Class Record view.
- Class Record printing now generates the official XLSX and opens it in the default spreadsheet app for printing.
- Term 1, Term 2 and Term 3 exports reuse the official layout and update the term label/sheet name automatically.
- Automatically fills Region, Division, School ID, School Name, School Year, Grade Level, Section, Teacher and Subject.
- Automatically fills HPS, raw scores, totals, PS, WS, component examination percentages, Initial Grade, Term Grade and Descriptor.
- Learners are placed in the template's MALE/FEMALE sections (up to 50 each).
- Export validation requires the official template's fixed 5 WW / 3 PT / 3 EX structure.
- The exported copy is static and standalone: original external INPUT DATA / HELPER links and calc-chain metadata are removed.
- Master template formatting, merged cells, drawings/logos, page setup, margins and print area are preserved.
- The master template is never overwritten.

E-Class Record App with GS and SF9 — v1.0.4

NEW
- Added Import Scores CSV in every Term Class Record.
- CSV importer auto-detects learner-name/LRN columns and common score headers such as WW1, PT1, ST1, and TE.
- Added a mapping screen so CSV columns from different offline Excel class-record formats can be matched to the app's current WW/PT/EXAM components before importing.
- Learners are matched by LRN first, then by normalized name.
- Optional: add unmatched learners from the CSV to the roster.
- Safe default preserves existing raw scores; optional overwrite can be enabled during import.
- Invalid numeric values and values above the app component HPS are skipped and reported.
- Imported raw scores automatically feed the existing PS, WS, Initial Grade, Term Grade, GS, and SF9 calculations.

E-Class Record App with GS and SF9

Version 1.0.3
- Added a global Upload / Change School Logo control under Setup.
- The selected school logo is used automatically in the Class Record, Grading Sheet, and SF9.
- School logo selection applies to every class in the app and is stored with the gradebook data/backups.
- Added Reset to Default for the school logo.
- Uploaded logos are resized to a maximum of 700 px and stored as PNG for efficient offline use.
- DepEd seal and DepEd wordmark remain fixed and unchanged.
- No changes to grade calculations, class data, GS/SF9 grading logic, or printing behavior.

Version 1.0.2
- Fixed the + New Class button in the Electron desktop app.
- Replaced the unsupported browser prompt() used by New Class with a native-looking in-app dialog.
- The new dialog supports Create Class, Cancel, Enter to submit, Escape to cancel, and required-name validation.
- Replaced the second prompt() dependency used by Roster > Paste names with the same desktop-safe dialog system.
- No changes to grade calculations, class data structure, GS/SF9 layouts, or saved-data format.

Version 1.0.1
- Fixed "This app doesn't support print preview" when using in-app Print buttons.
- Class Record and Grading Sheet now open the native Windows print dialog.
- Single-learner and all-learners SF9 printing now use the same native print path.
- Retained print backgrounds and the existing A4 print CSS/layout rules.
- No changes to grade calculations or stored gradebook data format.

Version 1.0.0
- Initial standalone Electron release.
