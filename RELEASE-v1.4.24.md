# KLAS v1.4.24

Teacher profile setup now opens in a modal popup with keyboard focus containment, Escape dismissal and a Close button. The greeting, photo and live clock sit in a rounded panel on the right. Three navigation cards sit at the bottom of the welcome composition; narrow windows stack them for readability. The bundled classroom remains visible with a left-to-right fade to a lighter right side. Welcome buttons have matching vector icons and retain readable labels.

Validated profile saving, photo persistence, official-name separation, modal open/Close/Escape, greeting periods, clock, Continue/startup skip, backup success/cancel, backup settings round trip, both themes at 320–1440 widths, reduced motion and print. The source and eight-file unsigned payload are checked for parity. No signed installation was performed.
