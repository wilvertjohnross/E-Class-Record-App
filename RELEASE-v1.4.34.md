# Release v1.4.34

- Keep the main sidebar in a fixed single-column layout.
- Prevent sidebar navigation overflow by allowing long labels to wrap within the sidebar width.
- Remove the narrow-window two-column sidebar behavior so nested navigation stays readable and consistent.
